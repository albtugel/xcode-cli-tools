# xcode-cli-tools 🚀

A collection of lightweight Zsh scripts to automate Xcode project navigation and CLI builds.

## Commands
- `xproj`: Interactive menu to find and open projects.
- `xrun`: Build and deploy to iOS Simulators with color-coded status.

## Installation
1. Clone the repo.
2. Source the files in your `~/.zshrc`.
